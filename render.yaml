services:
  - type: web
    name: adx-volatility-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python adx_volatility_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_USER_ID
        sync: false
